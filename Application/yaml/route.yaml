kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  user1git add-hello-world
spec:
  to:
    kind: Service
# set your user name
    name:  user1-hello-world
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None